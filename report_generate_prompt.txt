You are a senior frontend performance engineer.

Scan and fully analyze my React application source code.

Your mission:

Detect everything that can slow performance, increase bundle size, or cause unnecessary re-renders

Identify rendering issues (bad state usage, prop drilling, missing memoization, wrong keys, etc.)

Find lazy-loading mistakes (routes not split, heavy components eagerly loaded, missing Suspense)

Detect anti-patterns (inline functions in JSX, unnecessary effects, bad dependency arrays, global re-renders)

Check Error Boundaries, Suspense usage, and loading states

Flag network issues (over-fetching, waterfall requests, missing caching)

Spot bad folder structure or architectural decisions that hurt scalability

Identify anything that can cause white screens, broken rendering, or memory leaks

For each issue, include:

File path

Problem description (clear + short)

Why it’s harmful

Severity (Low / Medium / High / Critical)

Exact fix (code-level if possible)

Output the result as a single structured HTML file named report.html with:

A summary dashboard (total issues + severity count)

Collapsible sections per category

Clean, readable styling

No markdown, no explanations outside the report

Be strict. Assume this app is meant for production and real users.